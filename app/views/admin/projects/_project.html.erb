<tr>
  <td><%= project.team_id %></td>
  <td><%= link_to project.name, admin_project_path(project) %></td>
  <td><%= project.description %></td>
  <td><%= link_to t("user.show"), admin_project_path(project) %></td>
  <td><%= link_to t("user.edit"), edit_admin_project_path(project) %></td>
  <td><%= link_to t("user.delete"), admin_project_path(project), method: :delete,
    data: {confirm: t("user.confirm")} %></td>
</tr>