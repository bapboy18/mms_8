<% provide :title, 'All users' %>
<h1>All users</h1>

<%= will_paginate %>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>